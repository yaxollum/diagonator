An X11 compositor that draws diagonal lines across your screen to make you take a break from your computer.

## Build instructions
Run `make`.

## Usage
Run `./diagonator`.

If you get an error message saying that diagonator couldn't open the display, make sure that the `$DISPLAY` environment variable is set correctly.
